name: "CodeQL"

on:
  workflow_dispatch:

permissions: {}

jobs:
  placeholder:
    name: Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "CodeQL handled by GitHub default setup. This workflow exists to prevent conflicts."
